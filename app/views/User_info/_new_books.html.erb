<h3>New book</h3>
<%= form_with model: book do |f| %>
  <% if book.errors.any? %>
    <%= book.errors.count %> errors prohibited this obj from being saved:
    <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>
    <p>Book</p>
    <p><%= f.text_field :title %></p>
    <p>Opinion</p>
    <p><%= f.text_area :body, value: book.body %></p>
    <input type="submit" name="commit" value="Create Book" class="btn btn-success" data-disable-with="Create Book"<%= f.submit '投稿' %>>
 <% end %>
