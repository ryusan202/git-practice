<main>
        <p id="notice"></p>
        <div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
<tr></tr>
  <tr>
<%= render 'User_info/user_info', user: @user %>
<%= render 'User_info/new_books', book: @book %>
	  <th></th>
  </tr>
</tbody></table>
　<div class="form-group">
    
  </div>
</form>
    </div>
    <div class="col-md-8 offset-md-1">
      <h2>Users</h2>
      <table class="table">
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
        <% @users.each do |f| %>
    <tr>
    <td><%= image_tag f.get_profile_image(100,100) %></td>
    <td><%= f.name %></td>
    <td><%= link_to 'Show',  user_path(f) %></td>
     <% end %>
     </tr>
  </tbody>
</table>
    </div>
  </div>
</div>
      </main>