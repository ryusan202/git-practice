
        <p id="notice"></p>
        <div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  		<h1>Editing Book</h1>
<% if @book.errors.any? %>
<%= @book.errors.count %>error prohibited this obj from being saved:
  <ul>
<% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
  <%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <p><%= f.submit 'Update Book', url: book_path(@book), method: :put,class: "btn btn-sm btn-success" %></p>
<td><%= link_to 'Show', book_path(@book.id) %></td>
<tb>|</tb>
<td><%= link_to 'Back', books_path %></td>
<%end%>
  </div>
</div>
      </div>