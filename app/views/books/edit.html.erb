<h2>Editing Book</h2>
<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <p><%= f.submit 'Update Book', url: book_path(@book), method: :put %></p>
<td><%= link_to 'Show', book_path(@book.id) %></td>
<tb>|</tb>
<td><%= link_to 'Back', books_path %></td>
<%end%>